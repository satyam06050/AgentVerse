# Core dependencies - Phase 1

# Web scraping
requests>=2.31.0         # Send HTTP requests to websites
beautifulsoup4>=4.12.0   # Parse and extract HTML content
lxml>=4.9.0              # Fast HTML/XML parser backend

# LLM API
openai>=1.3.0            # OpenAI API client (supports DeepSeek via base_url)

# Environment management
python-dotenv>=1.0.0     # Load API keys from .env file

# Token handling
tiktoken>=0.5.0          # Token counting and chunk sizing

# Terminal UX / logging
rich>=13.5.0             # Better terminal output and debugging

# Future dependencies - Embeddings + RAG (Phase 2+)
sentence-transformers>=2.2.0    # Generate semantic embeddings
chromadb>=0.3.0                 # Vector database for RAG storage/retrieval
numpy>=1.24.0                   # Vector/math operations
scikit-learn>=1.3.0             # Cosine similarity and ML utilities
rank-bm25>=0.2.0                # Keyword-based retrieval (hybrid search)

# Future dependencies - Dynamic scraping (Phase 2+)
playwright>=1.40.0              # Browser automation for JS-heavy websites